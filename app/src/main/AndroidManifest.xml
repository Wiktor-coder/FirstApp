<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirstApp">
<!--        <activity-->
<!--            android:name=".fragment.EditPostFragment"-->
<!--            android:exported="false" />-->
<!--        &lt;!&ndash; .NewPostActivity используется внутри приложения поэтому exported="false" &ndash;&gt;-->
<!--        <activity-->
<!--            android:name=".fragment.NewPostFragment"-->
<!--            android:exported="false" />-->
<!--        <activity-->
<!--            android:name=".fragment.FeedFragment"-->
<!--            android:exported="true">-->

<!--        </activity> &lt;!&ndash; прописываем intents в соответствии с входными данными &ndash;&gt;-->
        <activity
            android:name=".activity.AppActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <service
            android:name=".service.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
    </application>

</manifest>